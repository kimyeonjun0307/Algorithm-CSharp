#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    string A;
    vector<string> s;
    while(cin>>A)
    {
        s.push_back(A);
    }
    cout<<s.size();
}