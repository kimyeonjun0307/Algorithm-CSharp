#include <bits/stdc++.h>
using namespace std;

int main()
 {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int A,B;
    
    cin>>A>>B;
    while(A!=0 && B!=0)
    {
        
        cout<<A+B<<"\n";
        cin>>A>>B;
    }

    return 0;
}